pkgname="neko-git"
short_desc="WIP package manager written in POSIX shell / utilities"
giturl="https://github.com/kawaiiamber/neko.git"
revision="1"
build_style="makefile"

# neko doesn't build
do_build()
{
	:
}

pkgname="neko"
short_desc="WIP package manager written in POSIX shell / utilities"
revision="1"
giturl="https://github.com/kamber-linux/neko.git"
build_style="makefile"

# neko doesn't build
do_build()
{
	:
}
